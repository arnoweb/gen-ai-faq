# Runtime dependencies for the Streamlit apps (faq_streamlit.py, faq_compare_streamlit.py,
# evaluate_models_compare.py). Compatible with Python 3.11-3.13.
#
# For fine-tuning a new model locally (autotrain-advanced), use requirements-train.txt
# instead, with Python 3.11 (autotrain-advanced pins sentencepiece==0.2.0, which has no
# Python 3.13 wheel).
streamlit==1.51.0
streamlit-searchbox==0.1.24
sentence-transformers==5.6.0
torch==2.9.1
onnxruntime==1.27.0
requests==2.32.3
python-dotenv==1.0.1
huggingface-hub==0.27.0
